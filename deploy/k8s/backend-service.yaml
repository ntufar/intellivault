apiVersion: v1
kind: Service
metadata:
  name: intellivault-backend-service
  labels:
    app: intellivault-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: intellivault-backend

apiVersion: v1
kind: Service
metadata:
  name: intellivault-frontend-service
  labels:
    app: intellivault-frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: intellivault-frontend

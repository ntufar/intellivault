apiVersion: apps/v1
kind: Deployment
metadata:
  name: intellivault-redis
  labels:
    app: intellivault-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: intellivault-redis
  template:
    metadata:
      labels:
        app: intellivault-redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          tcpSocket:
            port: 6379
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 6379
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: intellivault-redis
  labels:
    app: intellivault-redis
spec:
  type: ClusterIP
  ports:
  - port: 6379
    targetPort: 6379
    protocol: TCP
    name: redis
  selector:
    app: intellivault-redis
